import { Input } from '@chakra-ui/react';
import React from 'react';

const SearchBar = () => {
	return (
		<div>
			<Input placeholder="Enter a job title" height="72px" />
		</div>
	);
};

export default SearchBar;
